---
name: GitHub CLI Access
description: How to run gh commands from Claude's bash shell
type: reference
---
gh CLI is installed on Windows but not in Claude's bash PATH. Use the full path:

```
"/c/Program Files/GitHub CLI/gh.exe" <command>
```

Authenticated as TomerMassas via HTTPS.
